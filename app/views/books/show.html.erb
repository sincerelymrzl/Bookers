   <% if flash[:notice] %>
     <p id="notice">
       <%= flash[:notice] %>
     </p>
   <% end %>


<table>
  
  <thead>
    <tr>
      <th>Title:</th>
      <td><%= @book.title %></td>
</table>

<table>
      <th>Body:</th>
      <td><%= @book.body %></td>
    </tr>
  </thead>
</table>

        <td><%= link_to "Edit", edit_book_path(@book) %></td>
        <td><%= link_to "Back", books_path(@index) %></td>


